import 'package:flutter/material.dart';

const Gradient gradient = LinearGradient(
  colors: [
    Color(0xff294962),
    Color(0xff597e9b),
    Color(0xff1c1c1c)
    ],

  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);

const Gradient graDrawer = LinearGradient(
  colors: [Color(0xff4682B4), Colors.blueGrey],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);